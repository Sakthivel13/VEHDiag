# VEHDiag Design System — Icons

- Inline SVG, 24px grid, 1.75px stroke, round caps (Feather-style geometry) — independent original set.
- Icon set: gauge, activity (live), alert-triangle (DTC), code, cpu (ECU), fingerprint (VIN), snowflake (freeze frame), file-text (reports), bluetooth, usb, plug, database, battery, bolt (EV), motor, charger, shield, key, user, users, settings, bell, search, download, check, x, chevron-down, menu, sun/moon.
- Brand mark (`branding/icon.svg`): hex badge + V-check + pulse.
