"""Test suite of the Vehicle Diagnostics Platform.

The suite is split into ``unit`` (pure logic), ``integration`` (several layers
wired together), ``simulation`` (against the built-in ECU simulator) and
``ui_tests`` (pytest-qt, run with the offscreen Qt platform).
"""
from __future__ import annotations
