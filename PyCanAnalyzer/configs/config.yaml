app:
  name: pycananalyzer
  version: 0.1.0

pipeline:
  batch_size: 1024
  ring_buffer_size: 65536

devices:
  default: virtual

ml:
  model_paths:
    semantic: models/semantic.pkl

gui:
  refresh_rate: 50

logging:
  level: INFO
  # file: logs/pycan.log  # commented out to avoid file creation